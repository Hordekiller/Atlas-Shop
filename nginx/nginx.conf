limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;

# =============================================================================
# HTTPS Server (SSL/TLS) — commented out by default
# =============================================================================
# To enable HTTPS:
#   1. Obtain SSL certificates (e.g. certbot or self-signed)
#   2. Uncomment this entire server block
#   3. Mount certificates in docker-compose.yml:
#        volumes:
#          - ./nginx/ssl:/etc/nginx/ssl
#          - ./certbot/www:/var/www/certbot
#   4. Expose port 443 in docker-compose.yml:
#        ports:
#          - "443:443"
#   5. Uncomment the ACME challenge location in the HTTP server
#   6. Uncomment the HTTPS redirect in the HTTP server
#
# server {
#     listen 443 ssl;
#     server_name localhost;
#     charset utf-8;
#     client_max_body_size 50M;
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache   shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "DENY" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
#
#     location /api/ {
#         limit_req zone=api burst=50 nodelay;
#         proxy_pass http://api:8000/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#     }
#
#     location /uploads/ {
#         proxy_pass http://api:8000/uploads/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
#
#     location /api/docs {
#         proxy_pass http://api:8000/api/docs;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
#
#     location /admin/ {
#         proxy_pass http://admin:3001/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_buffering off;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     location / {
#         proxy_pass http://web:3000/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_buffering off;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

# =============================================================================
# HTTP Server (port 80)
# =============================================================================
server {
    listen 80;
    server_name localhost;
    charset utf-8;
    client_max_body_size 50M;

    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # ===== ACME Challenge (Let's Encrypt certbot verification) =====
    # Uncomment the following block when using certbot:
    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }

    # ===== HTTP → HTTPS Redirect =====
    # Uncomment the block below to redirect all HTTP traffic to HTTPS.
    # Requires the HTTPS server block above to be active.
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # API - NestJS
    location /api/ {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://api:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
    }

    # Uploaded files
    location /uploads/ {
        proxy_pass http://api:8000/uploads/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Swagger docs
    location /api/docs {
        proxy_pass http://api:8000/api/docs;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Admin panel
    location /admin/ {
        proxy_pass http://admin:3001/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_cache_bypass $http_upgrade;
    }

    # Web storefront
    location / {
        proxy_pass http://web:3000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_cache_bypass $http_upgrade;
    }
}
